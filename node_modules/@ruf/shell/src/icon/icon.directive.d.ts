import { MatIcon } from '@angular/material/icon';
import { OnChanges, SimpleChanges } from '@angular/core';
export declare class RufIconDirective implements OnChanges {
    private matIcon;
    icon: string;
    fisIcon: string;
    rufIcon: string;
    constructor(matIcon: MatIcon);
    ngOnChanges(changes: SimpleChanges): void;
}
