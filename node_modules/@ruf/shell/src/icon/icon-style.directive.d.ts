import { SimpleChanges, OnChanges, ElementRef, Renderer2 } from '@angular/core';
export declare const RUF_ICON_STYLE_VALUES: string[];
export declare class RufIconStyleDirective implements OnChanges {
    private renderer;
    private elementRef;
    private classPrefix;
    rufIconStyle: string;
    constructor(renderer: Renderer2, elementRef: ElementRef);
    ngOnChanges(changes: SimpleChanges): void;
    private _validateValue;
}
