import { ElementRef, EventEmitter } from '@angular/core';
import { CdkOverlayOrigin } from '@angular/cdk/overlay';
/**
 * Directive that can be applied on any element, and that toggles an attribute that can be used by the underlying element as need be.
 */
export declare class RufToggleDirective extends CdkOverlayOrigin {
    private elRef;
    value: boolean;
    chevron: boolean;
    arrow: boolean;
    readonly active: boolean;
    rufToggle: boolean;
    rufToggleChevron: boolean;
    rufToggleArrow: boolean;
    toggleChange: EventEmitter<any>;
    toggle: boolean;
    constructor(elRef: ElementRef);
    readonly normal: boolean;
    select(event: any): void;
    toggleValue(event: any): void;
    onClick(event: any): void;
    onKey(event: KeyboardEvent): void;
}
