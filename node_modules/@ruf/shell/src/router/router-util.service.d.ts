import { ComponentRef } from '@angular/core';
import { ActivatedRouteSnapshot, RouterOutlet } from '@angular/router';
export declare class RufRouterUtilService {
    private _routeComp;
    private _lastActivatedPath;
    private _lastActivatedOutlet;
    lastActivatedPath: string;
    lastActivatedOutlet: RouterOutlet;
    setComponentRef(path: string, componentRef: ComponentRef<any>): void;
    getComponentRef(path: any): ComponentRef<any>;
    fullPath(snapshot: ActivatedRouteSnapshot): string;
    getLeafNodePath(route: ActivatedRouteSnapshot): string;
    getLeafNode(route: any): any;
}
