import { ComponentFactoryResolver, ComponentRef, ChangeDetectorRef, ViewContainerRef } from '@angular/core';
import { RouterOutlet, ActivatedRoute, ChildrenOutletContexts } from '@angular/router';
import { RufRouterUtilService } from './router-util.service';
export declare class RufRouterOutletDirective extends RouterOutlet {
    private rufParentContexts;
    private rufLocation;
    private rufResolver;
    private rufName;
    private rufChangeDetector;
    private util;
    constructor(rufParentContexts: ChildrenOutletContexts, rufLocation: ViewContainerRef, rufResolver: ComponentFactoryResolver, rufName: string, rufChangeDetector: ChangeDetectorRef, util: RufRouterUtilService);
    detach(): ComponentRef<any>;
    activateWith(activatedRoute: ActivatedRoute, resolver: ComponentFactoryResolver | null): void;
    attach(ref: ComponentRef<any>, activatedRoute: ActivatedRoute): void;
    private storePreviousRoute;
}
