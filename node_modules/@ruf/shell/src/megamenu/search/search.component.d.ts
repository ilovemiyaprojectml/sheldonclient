import { ChangeDetectorRef, ElementRef, EventEmitter, OnDestroy } from '@angular/core';
import { RufShellIntl } from '../../utils/shell-intl';
export declare class RufMegamenuSearchComponent implements OnDestroy {
    private _intl;
    private _intlChanges;
    private _placeholder;
    private _clearSearchLabel;
    placeholder: string;
    clearSearchLabel: string;
    filterValue: EventEmitter<string>;
    inputFocus: EventEmitter<string>;
    filter: ElementRef;
    inputFocusClass: boolean;
    constructor(_intl: RufShellIntl, changeDetectorRef: ChangeDetectorRef);
    clear($event: any): void;
    handleKeyDown($event: any): void;
    ngOnDestroy(): void;
}
