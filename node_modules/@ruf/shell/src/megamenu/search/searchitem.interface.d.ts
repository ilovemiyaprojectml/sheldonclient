import { RufBaseItem } from '../../common/base-item';
export interface RufSearchItem extends RufBaseItem {
    label: string;
    children?: RufSearchItem[];
    context?: string;
    searchKey?: string;
}
