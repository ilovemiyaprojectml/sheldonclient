import { ChangeDetectorRef, EventEmitter, OnDestroy } from '@angular/core';
import { RufShellIntl } from '../utils/shell-intl';
export declare class RufSidePanelComponent implements OnDestroy {
    private _intl;
    private changeDetectorRef;
    private _intlChanges;
    private _pinLabel;
    private _unpinLabel;
    isPinned: boolean;
    togglePin: EventEmitter<{}>;
    pinLabel: string;
    unpinLabel: string;
    readonly toggleLabel: string;
    constructor(_intl: RufShellIntl, changeDetectorRef: ChangeDetectorRef);
    onPintoggle($event: any): void;
    ngOnDestroy(): void;
}
