import { ChangeDetectorRef, ElementRef, AfterContentInit, QueryList, OnDestroy } from '@angular/core';
import { RufFocusableDirective } from '../utils/focusable';
import { RufShellIntl } from '../utils/shell-intl';
import { RufMixinBase, RufCanColor } from '../common/color/color';
export declare class RufToolbarRowDirective {
}
export declare class RufToolbarComponent extends RufMixinBase implements AfterContentInit, OnDestroy, RufCanColor {
    _elementRef: ElementRef;
    private _intl;
    private _intlChanges;
    private _color;
    private _keyboardNav;
    private _ariaLabel;
    color: any;
    ariaLabel: string;
    private children;
    toolbarItems: QueryList<RufFocusableDirective>;
    constructor(_elementRef: ElementRef, _intl: RufShellIntl, changeDetectorRef: ChangeDetectorRef);
    ngAfterContentInit(): void;
    onKey(event: KeyboardEvent): void;
    ngOnDestroy(): void;
}
