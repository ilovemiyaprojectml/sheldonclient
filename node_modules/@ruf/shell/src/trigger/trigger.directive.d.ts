import { ElementRef, Renderer2, OnDestroy, AfterViewInit } from '@angular/core';
import { CdkOverlayOrigin } from '@angular/cdk/overlay';
import { RufTriggerTarget } from './trigger.interface';
export declare class RufTriggerDirective extends CdkOverlayOrigin implements AfterViewInit, OnDestroy {
    private elRef;
    private renderer;
    target: RufTriggerTarget;
    private subscription;
    constructor(elRef: ElementRef, renderer: Renderer2);
    ngAfterViewInit(): void;
    handleClickEvent(event: any): void;
    handleKeydownEvent(event: any): void;
    _checkTarget(): void;
    ngOnDestroy(): void;
}
