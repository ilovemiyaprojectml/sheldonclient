import { PipeTransform, OnDestroy } from '@angular/core';
import { RufLocaleService } from './locale.service';
export declare class RufCurrencyPipe implements PipeTransform, OnDestroy {
    private localeService;
    private currencyPipe;
    private subscription;
    constructor(localeService: RufLocaleService);
    ngOnDestroy(): void;
    transform(value: number, currencyCode: string, symbolDisplay: boolean, digits: number): number;
}
