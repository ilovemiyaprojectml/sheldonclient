import { RufMenubarComponent } from './menubar.component';
import { AfterContentInit, OnDestroy, ElementRef } from '@angular/core';
import { FocusableOption } from '@angular/cdk/a11y';
import { RufBaseItem } from '../common/base-item';
export interface RufMenubarItem extends RufBaseItem {
    data?: any;
    event?: any;
}
export declare class RufMenubarItemComponent implements RufMenubarItem, FocusableOption, AfterContentInit, OnDestroy {
    protected menubar: RufMenubarComponent;
    protected _elementRef?: ElementRef;
    private unsubscribe;
    path: string;
    data: any;
    selectable: boolean;
    menuitem: boolean;
    role: string;
    tabindex: number;
    active: boolean;
    ngAfterContentInit(): void;
    ngOnDestroy(): void;
    constructor(menubar: RufMenubarComponent, _elementRef?: ElementRef);
    onClick(event: any): void;
    focus(): void;
    matches(selectedPath: any): boolean;
}
