import { EventEmitter, AfterViewInit, ElementRef, OnChanges, OnDestroy, SimpleChanges, QueryList } from '@angular/core';
import { MatButtonToggle } from '@angular/material/button-toggle';
import { RufDynamicSegmentItem } from './dynamic-segment-item.model';
export declare class RufDynamicSegmentComponent implements AfterViewInit, OnChanges, OnDestroy {
    protected _elementRef: ElementRef;
    private _keyboardNav;
    private _selectedPath;
    private ativeItemClip;
    private subscription;
    items: RufDynamicSegmentItem[];
    showActiveItemClip: boolean;
    selectEmitter: EventEmitter<RufDynamicSegmentItem>;
    segmentItems: QueryList<MatButtonToggle>;
    navActions: any;
    selectedPath: string;
    constructor(_elementRef: ElementRef);
    ngAfterViewInit(): void;
    onKey(event: KeyboardEvent): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    addNewKeyboardNav(): void;
    /**
     * Set selectedPath and activate dynamic-segment item
     * @param item
     */
    selectSegmentItem(item: RufDynamicSegmentItem): void;
    /**
     * @returns - index of the selected dynamic-segment item. -1 if no dynamic-segment is selected.
     */
    selectedIndex(): number;
    private _matches;
}
