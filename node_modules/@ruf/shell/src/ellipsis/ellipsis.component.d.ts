import { ElementRef, EventEmitter, ChangeDetectorRef, OnDestroy, AfterViewChecked } from '@angular/core';
import { RufShellIntl } from '../utils/shell-intl';
import { BehaviorSubject } from 'rxjs';
export declare class RufEllipsisComponent implements AfterViewChecked, OnDestroy {
    private _intl;
    private changeDetectorRef;
    private _intlChanges;
    showReadMore$: BehaviorSubject<boolean>;
    text: string;
    lines: number;
    lineHeight: number;
    private _label;
    private _ariaLabel;
    label: string;
    ariaLabel: string;
    ellipsisContainer: ElementRef;
    expand: EventEmitter<{}>;
    constructor(_intl: RufShellIntl, changeDetectorRef: ChangeDetectorRef);
    checkReadMore(): void;
    ngAfterViewChecked(): void;
    ngOnDestroy(): void;
}
