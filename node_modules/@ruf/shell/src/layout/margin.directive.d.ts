import { OnChanges, SimpleChanges, Renderer2, ElementRef } from '@angular/core';
export declare const RUF_MARGIN_VALUES: string[];
export declare class RufMarginDirective implements OnChanges {
    private el;
    private renderer;
    private classPrefix;
    private _positions;
    rufMargin: string;
    rufMarginVertical: string;
    rufMarginHorizontal: string;
    rufMarginTop: string;
    rufMarginRight: string;
    rufMarginBottom: string;
    rufMarginLeft: string;
    margin: boolean;
    constructor(el: ElementRef, renderer: Renderer2);
    ngOnChanges(changes: SimpleChanges): void;
    private update;
    private _validateValue;
}
