import { EventEmitter } from '@angular/core';
import { MatMenuTrigger } from '@angular/material/menu';
import { RufTriggerTarget } from '../trigger/trigger.interface';
import { RufDynamicMenubarItem } from '../dynamic-menubar/dynamic-menubar-item';
export declare class RufMenuLabelComponent implements RufTriggerTarget {
    label: string;
    icon: string;
    children: RufDynamicMenubarItem[];
    toggle: EventEmitter<boolean>;
    select: EventEmitter<RufDynamicMenubarItem>;
    rowLayout: boolean;
    trigger: MatMenuTrigger;
    selectedPath: string;
    handleClick(event: any): void;
    hasPopup(): boolean;
    onClose(): void;
    handleKeydown(event: any): void;
}
