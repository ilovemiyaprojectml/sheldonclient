import { Template } from './template';
export interface DependencyManagerOptions {
    cachePath?: string;
}
export interface InstallOptions {
    link?: boolean;
    progress?: (msg: string, hash?: string, done?: boolean) => void;
}
export declare class DependencyManager {
    private counter;
    private _cachePath;
    readonly cachePath: string;
    constructor(opts?: DependencyManagerOptions);
    private createNodeModules;
    private linkDeps;
    private maybeInstallDeps;
    getNodeModulesPathForTemplate(template: Template, deps?: {
        [key: string]: string;
    }, opts?: InstallOptions, hash?: string): Promise<string>;
}
