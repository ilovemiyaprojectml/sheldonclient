import { TemplateBuildSettings } from '../template';
export interface FindOptions {
    cwd?: string;
    defaults?: any;
    assets?: string;
}
export interface ExampleInfo {
    dir: string;
}
export interface Example extends TemplateBuildSettings {
    [prop: string]: any;
}
export interface ExampleDef extends Example {
    ignore: string[];
}
export interface ReadmeDef {
    prop: string;
    data: string;
}
export declare function findExamples(opts: FindOptions, visit: (ex: Example, info: ExampleInfo) => void): Promise<void>;
export declare function findReadmes(opts: FindOptions, visit: (readme: ReadmeDef) => void): Promise<void>;
