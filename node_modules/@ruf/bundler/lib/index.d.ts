import { Template } from './template';
import { DependencyManagerOptions } from './dependency-manager';
export * from './finder';
export * from './template';
export * from './dependency-manager';
export { DetailedError } from './util';
export interface BundlerOptions extends DependencyManagerOptions {
}
export declare class Bundler {
    private dependencyManager;
    constructor(opts?: BundlerOptions);
    readonly cachePath: string;
    getTemplateNames(): Promise<string[]>;
    getTemplate(name: string): Template;
}
