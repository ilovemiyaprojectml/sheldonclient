// Puts a reference on the element in the scope.
// DO NOT USE: BAD PRACTICE
export default function () {
  return function (scope, element, attrs) {
    if (attrs.fisElement) {
      var obj = scope[attrs.fisElement];
      obj.$element = element;
    }
  };
}
