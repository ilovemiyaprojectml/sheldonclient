'use strict';

describe('fisTranslateFilter', function () {
  beforeEach(angular.mock.module('fis.core'));

  it('should invoke dummy translate filter', inject(function ($filter) {
    var translate = $filter('translate');
    expect(translate('abc.xyz')).toEqual('abc.xyz');
  }));
});
