#! /usr/bin/env node
const updateNotifier = require('update-notifier');
const meow = require('meow');
const pkg = require('../package');
const lib = require('./');

updateNotifier({ pkg }).notify();
const cli = meow('ruf-wrench', {});
lib.main(cli);
